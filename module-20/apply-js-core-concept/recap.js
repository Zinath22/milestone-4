//1. variable
// var myName = 'zinat';
// math operation : +, -, *, /
// shorthand : +=, --, *=, /=
// ++, --

    // (2)
    var friends = ['aa','bb', 'cc', 'dd', 'ee'];
    var thirdFriend = friends[2];
    friends[3] = 'dd';
    // (3) conditionals
    // >, <, >=, <=, ==, !=, !== 

    if(friends.length <2){
        console.log('fokir kono friend nai');
    }
    else{

    }

    //   (4) loop while
    var number = 0;
    while(number <=6){
        console.log(number);
        number++;
    }
    // for loop 
    for(var i =0; i<=10; i++){

    }

    //    (5) function 

    function isMoonUp(time){
        if(time > 7){
            return true;
        }
    }

    //  (6) object 
var jantus = {
    height : 6,
    weigth : 60,
    human : 'honest'

    
}
